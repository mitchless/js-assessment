if (typeof define !== 'function') { var define = require('amdefine')(module); }

define(function() {
  return {

    makeClosures : function(arr, fn) {

    },

    curryIt : function(fn) {

    }
  };
});
